#include <bits/stdc++.h>
#define int long long
#define ln "\n"
#define sp " "
#define rep(x, y) for (int x = 0; x < y; x++)

using namespace std;

string str, str2;

void solve() {

    getline(cin, str);
    getline(cin, str2);
    cout << str << " and " << str2 << " sitting in the tree" << ln;

}

signed main () {
    ios::sync_with_stdio(false);
    cin.tie(0);

    solve();

    return 0;
}
